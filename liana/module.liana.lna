(
    `omit (de)serializer`: false
    `schema tree`: | `set` {
        'list of characters': | `schema` (
            `schema imports`: {}
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {
                    'Output': (
                        `type`: | `single line` ~
                        `link`: | `no` ~
                    )
                }
                `simple types`: {
                    'Natural': (
                        `type`: | `number` (
                            `precision`: | `exact` (
                                `number of fractional digits`: _
                                `type`: | `natural` ~
                            )
                        )
                    )
                }
            )
            `modules`: {
                'List of Characters': (
                    `root value`: | `list` (
                        `value`: | `simple` | `global` 'Natural'
                        `results`: _
                    )
                )
            }
            `complexity`: | `unconstrained` ~
        )
        'fountain pen': | `schema` (
            `schema imports`: {
                'list of characters': (
                    `schema set child`: 'list of characters'
                    `schema`: ~
                )
            }
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {
                    'Output': (
                        `type`: | `single line` ~
                        `link`: | `no` ~
                    )
                }
                `simple types`: {
                    'boolean': (
                        `type`: | `boolean` ~
                    )
                }
            )
            `modules`: {
                'Paragraph': (
                    `root value`: | `state` (
                        `options`: {
                            'composed': (
                                `constraints`: _
                                `description`: _
                                `value`: | `list` (
                                    `value`: | `component` (
                                        `type`: | `internal` 'Paragraph'
                                        `results`: _
                                    )
                                    `results`: _
                                )
                            )
                            'sentences': (
                                `constraints`: _
                                `description`: _
                                `value`: | `list` (
                                    `value`: | `component` (
                                        `type`: | `internal` 'Sentence'
                                        `results`: _
                                    )
                                    `results`: _
                                )
                            )
                            'optional': (
                                `constraints`: _
                                `description`: _
                                `value`: | `optional` | `component` (
                                    `type`: | `internal` 'Paragraph'
                                    `results`: _
                                )
                            )
                            'nothing': (
                                `constraints`: _
                                `description`: _
                                `value`: | `nothing` ~
                            )
                            'rich list': (
                                `constraints`: _
                                `description`: _
                                `value`: | `group` {
                                    'items': (
                                        `description`: _
                                        `value`: | `list` (
                                            `value`: | `component` (
                                                `type`: | `internal` 'Sentence'
                                                `results`: _
                                            )
                                            `results`: _
                                        )
                                    )
                                    'if empty': (
                                        `description`: _
                                        `value`: | `optional` | `component` (
                                            `type`: | `internal` 'Sentence'
                                            `results`: _
                                        )
                                    )
                                    'if not empty': (
                                        `description`: _
                                        `value`: | `group` {
                                            'before': (
                                                `description`: _
                                                `value`: | `optional` | `component` (
                                                    `type`: | `internal` 'Sentence'
                                                    `results`: _
                                                )
                                            )
                                            'indent': (
                                                `description`: _
                                                `value`: | `simple` | `global` 'boolean'
                                            )
                                            'separator': (
                                                `description`: _
                                                `value`: | `optional` | `component` (
                                                    `type`: | `internal` 'Phrase'
                                                    `results`: _
                                                )
                                            )
                                            'after': (
                                                `description`: _
                                                `value`: | `optional` | `component` (
                                                    `type`: | `internal` 'Sentence'
                                                    `results`: _
                                                )
                                            )
                                        }
                                    )
                                }
                            )
                        }
                        `results`: _
                    )
                )
                'Sentence': (
                    `root value`: | `list` (
                        `value`: | `component` (
                            `type`: | `internal` 'Phrase'
                            `results`: _
                        )
                        `results`: _
                    )
                )
                'Phrase': (
                    `root value`: | `state` (
                        `options`: {
                            'value': (
                                `constraints`: _
                                `description`: _
                                `value`: | `state` (
                                    `options`: {
                                        'text': (
                                            `constraints`: _
                                            `description`: _
                                            `value`: | `text` | `global` 'Output'
                                        )
                                        'list of characters': (
                                            `constraints`: _
                                            `description`: _
                                            `value`: | `component` (
                                                `type`: | `external` (
                                                    `import`: 'list of characters'
                                                    `module`: 'List of Characters'
                                                )
                                                `results`: _
                                            )
                                        )
                                    }
                                    `results`: _
                                )
                            )
                            'indent': (
                                `constraints`: _
                                `description`: _
                                `value`: | `component` (
                                    `type`: | `internal` 'Paragraph'
                                    `results`: _
                                )
                            )
                            'composed': (
                                `constraints`: _
                                `description`: _
                                `value`: | `list` (
                                    `value`: | `component` (
                                        `type`: | `internal` 'Phrase'
                                        `results`: _
                                    )
                                    `results`: _
                                )
                            )
                            'optional': (
                                `constraints`: _
                                `description`: _
                                `value`: | `optional` | `component` (
                                    `type`: | `internal` 'Phrase'
                                    `results`: _
                                )
                            )
                            'nothing': (
                                `constraints`: _
                                `description`: _
                                `value`: | `nothing` ~
                            )
                            'rich list': (
                                `constraints`: _
                                `description`: _
                                `value`: | `group` {
                                    'items': (
                                        `description`: _
                                        `value`: | `list` (
                                            `value`: | `component` (
                                                `type`: | `internal` 'Phrase'
                                                `results`: _
                                            )
                                            `results`: _
                                        )
                                    )
                                    'if empty': (
                                        `description`: _
                                        `value`: | `component` (
                                            `type`: | `internal` 'Phrase'
                                            `results`: _
                                        )
                                    )
                                    'if not empty': (
                                        `description`: _
                                        `value`: | `group` {
                                            'before': (
                                                `description`: _
                                                `value`: | `component` (
                                                    `type`: | `internal` 'Phrase'
                                                    `results`: _
                                                )
                                            )
                                            'separator': (
                                                `description`: _
                                                `value`: | `component` (
                                                    `type`: | `internal` 'Phrase'
                                                    `results`: _
                                                )
                                            )
                                            'after': (
                                                `description`: _
                                                `value`: | `component` (
                                                    `type`: | `internal` 'Phrase'
                                                    `results`: _
                                                )
                                            )
                                        }
                                    )
                                }
                            )
                        }
                        `results`: _
                    )
                )
            }
            `complexity`: | `unconstrained` ~
        )
        'get instream data': | `schema` (
            `schema imports`: {}
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {
                    'multi line text': (
                        `type`: | `multi line` ~
                        `link`: | `no` ~
                    )
                }
                `simple types`: {}
            )
            `modules`: {
                'Parameters': (
                    `root value`: | `nothing` ~
                )
                'Result': (
                    `root value`: | `text` | `global` 'multi line text'
                )
            }
            `complexity`: | `unconstrained` ~
        )
        'log error': | `schema` (
            `schema imports`: {
                'fountain pen': (
                    `schema set child`: 'fountain pen'
                    `schema`: ~
                )
            }
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {}
                `simple types`: {}
            )
            `modules`: {
                'Parameters': (
                    `root value`: | `group` {
                        'message': (
                            `description`: _
                            `value`: | `component` (
                                `type`: | `external` (
                                    `import`: 'fountain pen'
                                    `module`: 'Paragraph'
                                )
                                `results`: _
                            )
                        )
                    }
                )
                'Error': (
                    `root value`: | `nothing` ~
                )
            }
            `complexity`: | `unconstrained` ~
        )
        'log': | `schema` (
            `schema imports`: {
                'fountain pen': (
                    `schema set child`: 'fountain pen'
                    `schema`: ~
                )
            }
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {}
                `simple types`: {}
            )
            `modules`: {
                'Parameters': (
                    `root value`: | `group` {
                        'message': (
                            `description`: _
                            `value`: | `component` (
                                `type`: | `external` (
                                    `import`: 'fountain pen'
                                    `module`: 'Paragraph'
                                )
                                `results`: _
                            )
                        )
                    }
                )
                'Error': (
                    `root value`: | `nothing` ~
                )
            }
            `complexity`: | `unconstrained` ~
        )
        'write to stderr': | `schema` (
            `schema imports`: {}
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {
                    'text': (
                        `type`: | `single line` ~
                        `link`: | `no` ~
                    )
                }
                `simple types`: {}
            )
            `modules`: {
                'Parameters': (
                    `root value`: | `text` | `global` 'text'
                )
            }
            `complexity`: | `unconstrained` ~
        )
        'write to stdout': | `schema` (
            `schema imports`: {}
            `resolver imports`: {}
            `globals`: (
                `complexity`: | `unconstrained` ~
                `text types`: {
                    'text': (
                        `type`: | `single line` ~
                        `link`: | `no` ~
                    )
                }
                `simple types`: {}
            )
            `modules`: {
                'Parameters': (
                    `root value`: | `text` | `global` 'text'
                )
            }
            `complexity`: | `unconstrained` ~
        )
    }
)
